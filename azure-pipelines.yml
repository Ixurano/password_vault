trigger:
- master

pool:
  vmImage: ubuntu-latest

steps:
- task: UsePythonVersion@0
  inputs:
    versionSpec: '3.9'
  displayName: 'Use Python 3.9'

- task: CmdLine@2
  inputs:
    script: |
      python -m venv env
      source env/bin/activate
      pip install --upgrade pip
      pip install -r requirements.txt
  displayName: 'Install dependencies'

- task: CmdLine@2
  inputs:
    script: |
      pyinstaller --onefile password_vault.py -w
  displayName: 'Build executable'

- task: CopyFiles@2
  inputs:
    Contents: 'dist/password_vault'
    TargetFolder: '$(Build.ArtifactStagingDirectory)'
  displayName: 'Copy artifact'

- task: PublishPipelineArtifact@0
  inputs:
    artifactName: 'password_vault'
    targetPath: '$(Build.ArtifactStagingDirectory)'
  displayName: 'Publish artifact'

- task: AzureFileCopy@4
  inputs:
    SourcePath: '$(Pipeline.Workspace)/password_vault'
    azureSubscription: 'QocCf2HL+acByGocsbkilNUPq7vj3HYmW48kWkU8kPU0sFGlkklKdML+S6lfp5SfwGNvh64hDcP0+AStPqufkw=='
    Destination: 'AzureBlob'
    storage: 'devops2023passwordvault'
    ContainerName: 'passwordvault'
